<h1><%= pluralize(@users.size, "User") %> Registered</h1>
<ul class="user">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
      created
      <%= time_ago_in_words(user.created_at) %> ago
    </li>
  <% end %>
</ul>
